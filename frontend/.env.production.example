# Vercel → Project → Settings → Environment Variables
# Set for Production (and Preview if you use preview URLs; point API at a preview or prod backend).

NEXT_PUBLIC_API_URL=https://resell-radar-api.onrender.com
